generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"] // <--- MUST add this line
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}