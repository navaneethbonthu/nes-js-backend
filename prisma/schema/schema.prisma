generator client {
  provider        = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-1.0.x"] // Add this line
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}